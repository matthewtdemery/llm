# Branch v6

This branch contains the MoviesMadeEasy / FindAMovie v6 streaming-first prototype synced from servering www-dev.

Primary app path in the repo:

```text
mvp/findamovie_mvp_static/index.html
```

Versioned v6 app copy:

```text
mvp/findamovie_mvp_static/v6/index.html
```

Notes:

- v6 separates streaming/rental discovery from theater/showtime routing.
- GitHub version workflow uses branches (`v1`, `v2`, `v3`, `v4`, `v6`) plus version folders for static demo navigation.
- This branch was synced from `/srv/llmpoc/mcp_exec_server/www-dev/findamovie` and scrubbed of servering-only dev overlay references.
